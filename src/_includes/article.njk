---
layout: 'base.njk'
---

<article class="main-article">
<div class="container container--narrow flow">
  
    <h1 class="article-title">
      {{ title }}
    </h1>
    <figure class="main-article__figure">
      <img src="{{ image }}" alt="{{ imageAlt }}">
      <figcaption>
        {{ imageAlt }}
      </figcaption>
    </figure>
    
    {{ content | safe }}

    {%- for imager in images -%}
      {%set imgP = imager.src %}
      {% if videoB %} 
        <video playsinline="" loop="loop" autoplay="autoplay" muted="muted" src="{{ imgP }}"></video>
      {% else %}
        {%set imgA = imager.alt %}
        {% Image ""+imgP, ""+imgA  %}
      {% endif %}
    {%- endfor -%}

</div>
</article>
